#include "pch.h"

/*
    This is my own example based on example from Refactoring Guru
    
    The main rule of Abstract Method is to override an abstract method 
    that returns (abstract type / interface)
*/

int main()
{
    Microsoft().MakeComputer();
    Apple().MakeComputer();
}